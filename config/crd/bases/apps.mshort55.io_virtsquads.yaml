---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: virtsquads.apps.mshort55.io
spec:
  group: apps.mshort55.io
  names:
    kind: VirtSquad
    listKind: VirtSquadList
    plural: virtsquads
    singular: virtsquad
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: VirtSquad is the Schema for the virtsquads API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VirtSquad
            properties:
              kike:
                description: Kike defines configuration for Kike's pods
                properties:
                  name:
                    description: Name specifies the name for the team member's pod
                    type: string
                  replicas:
                    default: 1
                    description: Replicas specifies the number of pods for this team
                      member
                    format: int32
                    type: integer
                type: object
              kurtis:
                description: Kurtis defines configuration for Kurtis's pods
                properties:
                  name:
                    description: Name specifies the name for the team member's pod
                    type: string
                  replicas:
                    default: 1
                    description: Replicas specifies the number of pods for this team
                      member
                    format: int32
                    type: integer
                type: object
              matt:
                description: Matt defines configuration for Matt's pods
                properties:
                  name:
                    description: Name specifies the name for the team member's pod
                    type: string
                  replicas:
                    default: 1
                    description: Replicas specifies the number of pods for this team
                      member
                    format: int32
                    type: integer
                type: object
              oksana:
                description: Oksana defines configuration for Oksana's pods
                properties:
                  name:
                    description: Name specifies the name for the team member's pod
                    type: string
                  replicas:
                    default: 1
                    description: Replicas specifies the number of pods for this team
                      member
                    format: int32
                    type: integer
                type: object
            type: object
          status:
            description: status defines the observed state of VirtSquad
            properties:
              kikePods:
                description: KikePods tracks the names of created pods for Kike
                items:
                  type: string
                type: array
              kurtisPods:
                description: KurtisPods tracks the names of created pods for Kurtis
                items:
                  type: string
                type: array
              mattPods:
                description: MattPods tracks the names of created pods for Matt
                items:
                  type: string
                type: array
              oksanaPods:
                description: OksanaPods tracks the names of created pods for Oksana
                items:
                  type: string
                type: array
              readyPods:
                description: ReadyPods tracks the total number of ready pods
                format: int32
                type: integer
              totalPods:
                description: TotalPods tracks the total number of pods
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
